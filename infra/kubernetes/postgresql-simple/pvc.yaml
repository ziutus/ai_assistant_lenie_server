apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dbvol
spec:
  storageClassName: gp2
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce